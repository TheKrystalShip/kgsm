schema_version: 1
name: cssource
runtime: native
metadata:
  # TODO: curate — advisory values researched per game; null = unknown/unbounded, NEVER 0
  display_name: "Counter-Strike: Source"
  description: "Counter-Strike: Source is a tactical first-person shooter by Valve that remakes the original Counter-Strike on the Source engine, pitting teams of terrorists and counter-terrorists against each other across objective-based rounds."
  rawg_slug: "counter-strike-source"
  max_players: 32
  min_ram_mb: 512
  recommended_ram_mb: 1024
  base_disk_mb: 2200
# RCON polling — an optional second source of player presence, for a game whose
# console does not announce joins and leaves in its log, or announces only one of
# the two. Empty rcon_port leaves it off, which is what every unset field here
# means: the capability is available to this game, not yet wired up for it.
# Nothing below is guessed — fill these in only from a server observed answering
# them, the same rule the presence patterns follow.
rcon_port: ""
rcon_password: ""
rcon_poll_interval_seconds: 10
# The command that lists connected players, and how to read one player out of what
# it answers — matched per line, with optional named groups `id` and `name`. Games
# word their rosters differently, so carrying the pattern here is what lets one
# poller read any of them without knowing which game it is talking to.
rcon_players_command: ""
rcon_players_regex: ''
native:
  ports: 26900/udp|27015/tcp|27015/udp|27020/udp|27005/udp
  steam_app_id: 232330
  client_steam_app_id: 240
  steamcmd_arguments: ""
  is_steam_account_required: false
  platform: linux
  level_name: de_dust
  executable_subdirectory: ""
  executable_file: srcds_run
  executable_arguments: -console -game cstrike -secure +maxplayers 22 +map $instance_level_name
  # This server does not read its stdin: a command written to the console FIFO is never
  # consumed. Declaring one would spend the whole stop timeout on a line nobody reads before
  # the stop falls through to a signal, which this game answers in milliseconds.
  stop_command: ""
  save_command: ""
  startup_success_regex: Server started
